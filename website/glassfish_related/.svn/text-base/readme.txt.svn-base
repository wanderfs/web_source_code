To get glassfish work with the database, 

You should:

1. Override the config directory in the corresponding domain (e.g glassfish/domains/domain1/config).

2. Add the mysql-*.jar in the library of glassfish (e.g. glassfish/lib/)

